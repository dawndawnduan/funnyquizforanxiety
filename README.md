# FunnyQuiz - 焦虑社会生存姿势测试

一个年轻化、轻巧、简约的 H5 心理测试应用，通过 9 道题目测试用户在焦虑社会中的生存姿势类型。

## 功能特性

- 📱 移动端友好的 H5 设计
- 🎨 年轻化、简约的 UI 风格
- ⚡ 流畅的动画交互（Framer Motion）
- 📊 实时计分算法和类型判定
- 🎭 6 种人格类型详细分析
- 📸 一键生成分享海报
- 📈 访问量统计（每日新增）

## 技术栈

### 前端
- React 18 + TypeScript
- Vite（构建工具）
- Tailwind CSS（样式）
- Framer Motion（动画）
- React Router（路由）
- html2canvas（海报生成）

### 后端
- Node.js + Express
- TypeScript
- SQLite3（数据库）
- PM2（进程管理）

### 部署
- Nginx（反向代理）
- 阿里云 ECS

## 快速开始

### 1. 克隆项目
```bash
git clone <your-repo-url>
cd funnyquiz
```

### 2. 安装依赖
```bash
# 前端
cd client
npm install

# 后端
cd ../server
npm install
```

### 3. 本地开发
```bash
# 终端 1 - 启动前端开发服务器
cd client
npm run dev

# 终端 2 - 启动后端开发服务器
cd server
npm run dev
```

访问 `http://localhost:5173` 查看效果

### 4. 构建生产版本
```bash
# 构建前端
cd client
npm run build

# 构建后端
cd server
npm run build
```

## 部署到生产环境

详细的部署步骤请查看 [DEPLOYMENT.md](./DEPLOYMENT.md)

### 快速部署步骤
1. 配置阿里云 ECS 安全组（开放 80/443 端口）
2. 安装 PM2: `npm install -g pm2`
3. 上传代码到服务器
4. 安装依赖并构建
5. 配置 Nginx
6. 启动应用: `pm2 start deploy/ecosystem.config.js`

## 项目结构

```
funnyquiz/
├── client/                    # 前端项目
│   ├── src/
│   │   ├── pages/            # 页面组件（Home, Quiz, Result）
│   │   ├── components/       # 通用组件
│   │   ├── data/
│   │   │   ├── questions.ts  # 9道测试题目
│   │   │   └── personalities.ts # 6种人格类型数据
│   │   ├── utils/
│   │   │   ├── calculator.ts # 计分算法
│   │   │   └── api.ts        # API请求
│   │   ├── types/            # TypeScript类型定义
│   │   └── App.tsx
│   └── package.json
│
├── server/                    # 后端项目
│   ├── src/
│   │   ├── index.ts          # Express服务器入口
│   │   ├── routes/
│   │   │   └── stats.ts      # 统计接口
│   │   └── db/
│   │       └── database.ts   # SQLite操作
│   ├── data/
│   │   └── stats.db          # SQLite数据库
│   └── package.json
│
├── deploy/                    # 部署配置
│   ├── nginx.conf            # Nginx配置
│   └── ecosystem.config.js   # PM2配置
│
├── DEPLOYMENT.md             # 部署文档
└── README.md                 # 项目说明
```

## 6 种人格类型

1. **I 躺平派** 🛋️ - 佛系随缘（低确定 × 低速度）
2. **II 保守派** 🛡️ - 稳健守成（高确定 × 低速度）
3. **III 发泄派** ⚡ - 燃尽爆发（低确定 × 高速度）
4. **IV 激进派** 🚀 - 目标达人（高确定 × 高速度）
5. **V 平衡派** ⚖️ - 从容中庸（中确定 × 中速度）
6. **VI 游移派** 🌊 - 混沌流浪（波动的大起大落）

## API 接口

### 统计接口
- `POST /api/stats/visit` - 记录访问
- `GET /api/stats/today` - 获取今日统计
- `GET /api/stats/recent?days=7` - 获取最近N天统计
- `GET /api/stats/total` - 获取总访问量
- `GET /api/health` - 健康检查

## 开发说明

### 环境要求
- Node.js >= 18
- npm or pnpm
- Nginx（生产环境）
- PM2（生产环境）

### 环境变量
前端 `client/.env`:
```env
VITE_API_BASE_URL=    # API地址，留空使用相对路径
```

后端 `server/.env`:
```env
PORT=3001              # 服务器端口
NODE_ENV=production    # 环境
```

## 常见问题

### 1. 如何查看访问统计？
```bash
# 通过 API
curl http://your-domain.com/api/stats/today

# 或直接查询数据库
sqlite3 server/data/stats.db "SELECT * FROM daily_stats;"
```

### 2. 如何修改题目或人格类型？
编辑以下文件：
- 题目：`client/src/data/questions.ts`
- 人格类型：`client/src/data/personalities.ts`

### 3. 如何自定义主题颜色？
编辑 `client/tailwind.config.js` 中的 colors 配置

## 许可证

MIT License

## 作者

Your Name

## 贡献

欢迎提交 Issue 和 Pull Request！
